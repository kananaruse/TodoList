<p>検索</p>
<%= form_tag(search_lists_path,:method => 'get') do %>
  <%= text_field_tag :search %>
  <%= submit_tag 'Search', :title => nil %>
<% end %>

<p>ToDoが<%= @tasks.count %>件見つかりました</p>
<table>
  <tbody>
    <% @tasks.each do |task| %>
      <tr>
        <th><%= link_to task.name, list_url(task.list_id) %></th>
        <td align = "right">期限：<%= task.due_date %></td>
      </tr>
      <tr>
        <td align = "right">リスト：<%= task.list.title %></td>
        <td align = "right">作成日：<%= task.created_at.strftime("%Y-%m-%d") %></td>
      </tr>
    <% end %>
  </tbody>
</table>


<p>ToDoリストが<%= @lists.count %>件見つかりました</p>
<table>
  <tbody>
    <% @lists.each do |list| %>
      <tr>
        <td><%= link_to list.title, list %></td>
        <td align = "right">作成日：<%= list.created_at.strftime("%Y-%m-%d") %></td>
      </tr>
    <% end %>
  </tbody>
</table>
